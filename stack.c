#include "monty.h"
/**
 * d_stack - the prints the top
 * @head: the stack head
 * @counter: line_number
 * Return: no return
*/
void d_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
